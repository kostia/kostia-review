<%= scrivito_tag(:h1, widget, :headline) %>

<%= scrivito_tag(:div, widget, :content) %>

<% if link = widget.link_to %>
  <p>
    <%= link_to(scrivito_path(link), class: 'btn btn-primary btn-lg') do %>
      <%= link.title %>
    <% end %>
  </p>
<% end %>
