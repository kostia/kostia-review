<div class="google-maps">
  <% if scrivito_editor_authenticated? %>
    <%= cms_tag(:input, widget, :address, type: 'text', data: { editor: 'google-maps' }) %>
  <% end %>

  <div class="map" data-location="<%= widget.address %>"></div>
</div>
